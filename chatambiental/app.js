const express = require('express'); const cors = require('cors'); const app = express(); const PORT = 3001; app.use(cors()); app.use(express.json()); app.use(express.static('public')); app.get('/', (req, res) => { res.send('Chatbot Ambiental - Funcionando!'); }); app.post('/api/chat', (req, res) => { const { message } = req.body; let resposta = 'Olá! Sou o Chatbot Ambiental. Como posso ajudar?'; if (message && message.toLowerCase().includes('descomissionamento')) { resposta = 'Descomissionamento é o processo de desativação de instalações de petróleo e gás.'; } res.json({ success: true, response: resposta }); }); app.listen(PORT, () => { console.log(' Servidor rodando na porta ' + PORT); });
