# Force npm to install all dependencies
legacy-peer-deps=false
strict-peer-dependencies=false
engine-strict=false

# Ensure build runs properly
save-exact=false
package-lock=true

# Registry
registry=https://registry.npmjs.org/
